import 'src/bootstrap.dart';

/// Entry point.
///
/// All real startup work lives in `bootstrap()` so it can be re-used
/// from integration tests with overrides. Keep this file boring.
void main() => bootstrap();
